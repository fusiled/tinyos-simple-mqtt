module NodeC
{
	uses
	{
		interface Packet;
                interface AMPacket;
                interface AMSend;
                interface SplitControl as AMControl;
                interface Timer<TMilli> as MilliTimer;
                interface Receive;
	}
}
implementation
{


}
